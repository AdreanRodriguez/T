{
  "name": "aws-cli-api-local",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "tar -acf index.zip index.js && aws lambda update-function-code --function-name AWS-CLI-API-Local --zip-file fileb://index.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
